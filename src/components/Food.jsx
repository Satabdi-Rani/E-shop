// import React from 'react';
import {data} from '../data/data.js';

const Food = () => {
    console.log(data);
    return (
        <div>
            Food
        </div>
    );
};

export default Food;